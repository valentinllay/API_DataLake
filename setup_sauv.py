from setuptools import setup, find_packages

setup(
    name="hello_world",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # par exemple "Flask>=2.2.5" si tu en as besoin
    ],
)